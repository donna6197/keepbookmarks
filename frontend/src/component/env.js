module.exports = {
    url: function() {
        return 'http://127.0.0.1:5000'  // example
        //uncomment the line below and enter replace [URL AND PORT] with your details.  Use the format in the example above
        // return 'http://[URL AND PORT]'
    }
}
